<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dev.ainer.module.notification.notification.infrastructure.NotificationTemplateMapper">

    <resultMap id="TemplateRowMap" type="dev.ainer.module.notification.notification.infrastructure.NotificationTemplateRow">
        <id property="id" column="id"/>
        <result property="code" column="code"/>
        <result property="channel" column="channel"/>
        <result property="titleTemplate" column="title_template"/>
        <result property="bodyTemplate" column="body_template"/>
        <result property="variablesSchema" column="variables_schema"/>
        <result property="status" column="status"/>
        <result property="version" column="version"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="insertReturningId" resultType="java.util.UUID">
        INSERT INTO ainer_notification_template (
            id, code, channel, title_template, body_template, variables_schema, status, version, created_at, updated_at
        ) VALUES (
            #{row.id}, #{row.code}, #{row.channel}, #{row.titleTemplate}, #{row.bodyTemplate},
            #{row.variablesSchema}::jsonb, #{row.status}, #{row.version}, #{now}, #{now}
        )
        RETURNING id
    </select>

    <select id="selectActiveByCode" resultMap="TemplateRowMap">
        SELECT id, code, channel, title_template, body_template,
               variables_schema::text AS variables_schema, status, version, created_at, updated_at
        FROM ainer_notification_template
        WHERE code = #{code} AND status = 'ACTIVE'
    </select>

</mapper>

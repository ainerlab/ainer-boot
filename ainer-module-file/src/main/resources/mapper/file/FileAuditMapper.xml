<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dev.ainer.module.file.file.infrastructure.FileAuditMapper">

    <insert id="insert">
        INSERT INTO ainer_file_audit (
            id, file_id, operation, namespace, actor_issuer, actor_type, actor_id, request_id,
            occurred_at
        ) VALUES (
            #{row.id}, #{row.fileId}, #{row.operation}, #{row.namespace}, #{row.actorIssuer},
            #{row.actorType}, #{row.actorId}, #{row.requestId}, #{row.occurredAt}
        )
    </insert>

</mapper>

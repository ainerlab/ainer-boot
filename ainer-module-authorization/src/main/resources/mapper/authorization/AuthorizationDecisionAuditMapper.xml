<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dev.ainer.authorization.infrastructure.AuthorizationDecisionAuditMapper">

    <insert id="insert">
        INSERT INTO ainer_authorization_decision_audit (
            decision_id, workspace_id,
            requester_issuer, requester_type, requester_id,
            permission_code, resource_type, resource_id,
            outcome, reason_code, policy_version,
            request_id, trace_id, evaluated_at
        ) VALUES (
            #{row.decisionId}, #{row.workspaceId},
            #{row.requesterIssuer}, #{row.requesterType}, #{row.requesterId},
            #{row.permissionCode}, #{row.resourceType}, #{row.resourceId},
            #{row.outcome}, #{row.reasonCode}, #{row.policyVersion},
            #{row.requestId}, #{row.traceId}, #{row.evaluatedAt}
        )
    </insert>

</mapper>

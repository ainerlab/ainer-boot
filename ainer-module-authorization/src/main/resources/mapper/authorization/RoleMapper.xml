<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dev.ainer.authorization.infrastructure.RoleMapper">

    <resultMap id="RoleRowMap" type="dev.ainer.authorization.infrastructure.RoleRow">
        <id property="id" column="id"/>
        <result property="code" column="code"/>
        <result property="name" column="name"/>
        <result property="systemRole" column="system_role"/>
        <result property="status" column="status"/>
        <result property="version" column="version"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="insertReturningId" resultType="java.util.UUID">
        INSERT INTO ainer_authorization_role (code, name, system_role, status, version, created_at, updated_at)
        VALUES (#{row.code}, #{row.name}, #{row.systemRole}, #{row.status}, #{row.version}, #{now}, #{now})
        RETURNING id
    </select>

    <select id="selectById" resultMap="RoleRowMap">
        SELECT id, code, name, system_role, status, version, created_at, updated_at
        FROM ainer_authorization_role
        WHERE id = #{id}
    </select>

    <select id="selectActiveByCode" resultMap="RoleRowMap">
        SELECT id, code, name, system_role, status, version, created_at, updated_at
        FROM ainer_authorization_role
        WHERE code = #{code} AND status = 'ACTIVE'
    </select>

    <select id="selectAll" resultMap="RoleRowMap">
        SELECT id, code, name, system_role, status, version, created_at, updated_at
        FROM ainer_authorization_role
        ORDER BY created_at, id
    </select>

    <delete id="deletePermissions">
        DELETE FROM ainer_authorization_role_permission WHERE role_id = #{roleId}
    </delete>

    <insert id="insertPermissions">
        INSERT INTO ainer_authorization_role_permission (role_id, permission_code, created_at)
        VALUES
        <foreach collection="codes" item="code" separator=", ">
            (#{roleId}, #{code}, #{now})
        </foreach>
    </insert>

    <update id="bumpVersion">
        UPDATE ainer_authorization_role
        SET version = version + 1, updated_at = #{now}
        WHERE id = #{roleId} AND version = #{expectedVersion}
    </update>

    <select id="selectPermissionCodes" resultType="java.lang.String">
        SELECT permission_code
        FROM ainer_authorization_role_permission
        WHERE role_id = #{roleId}
        ORDER BY permission_code
    </select>

</mapper>

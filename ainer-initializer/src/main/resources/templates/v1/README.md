# {{project.name}}

{{project.description}}

Generated by the Ainer Project Initializer (manifest v1, ADR-0035). This project is an
independent consumer of published Ainer artifacts; it contains no Ainer source copies.

- Ainer BOM: `dev.ainer:ainer-dependencies:{{ainner.version}}`
- Spring Boot: {{spring.boot.version}}
- Java: {{java.release}}{{owner.block}}

## Run

```
./mvnw spring-boot:run
```

- Health / smoke: GET http://localhost:8080/api/ping
- Framework rules apply: HTTP status is authoritative, every response carries `X-Request-Id`,
  never log credentials, tokens or provider payloads.

## Build & Test

```
./mvnw verify
```
spring:
  application:
    name: {{project.artifactId}}
  main:
    banner-mode: "off"
{{database.config}}

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: never
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dev.ainer.module.identity.foundation.HumanProfileMapper">

    <resultMap id="humanProfileRow" type="dev.ainer.module.identity.foundation.HumanProfileRow">
        <id property="accountId" column="account_id"/>
        <result property="displayName" column="display_name"/>
        <result property="avatarUrl" column="avatar_url"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="selectByAccountId" resultMap="humanProfileRow">
        SELECT account_id, display_name, avatar_url, updated_at
        FROM ainer_identity_human_profile
        WHERE account_id = #{accountId}
    </select>

    <insert id="insertProfile"
            parameterType="dev.ainer.module.identity.foundation.HumanProfileRow">
        INSERT INTO ainer_identity_human_profile
            (account_id, display_name, avatar_url, updated_at)
        VALUES
            (#{accountId}, #{displayName}, #{avatarUrl}, #{updatedAt})
    </insert>

    <update id="updateProfile"
            parameterType="dev.ainer.module.identity.foundation.HumanProfileRow">
        UPDATE ainer_identity_human_profile
        SET display_name = #{displayName},
            avatar_url = #{avatarUrl},
            updated_at = #{updatedAt}
        WHERE account_id = #{accountId}
    </update>
</mapper>